/**
 * App shell placeholder — dashboard shell จริง (layout, sidebar, auth, nav)
 * จะถูกสร้างในขั้นที่ 8
 */
export function App() {
  return (
    <div className="min-h-screen flex items-center justify-center bg-slate-50 text-slate-700">
      <div className="text-center">
        <h1 className="text-2xl font-semibold">TPG Center</h1>
        <p className="mt-2 text-slate-500">ระบบติดตามงานคลังสินค้าและขนส่ง</p>
        <p className="mt-1 text-sm text-slate-400">Dashboard shell — ขั้นที่ 8</p>
      </div>
    </div>
  );
}
